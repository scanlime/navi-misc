- make GameConnection use the MainListener interface, and build a FrameListener->MainLoop adaptor
- refactor things to remove the Ogre dependency from the server
- give MainLoop a way to do timeouts, rather than calling things every single frame (for things like network, info panel)
