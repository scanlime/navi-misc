* Trying to connect to a server that doesn't exist breaks it
  horribly.
* Some commands like /close don't seem to work
* Sending a message when you're not in a channel breaks it too.
* Connection bug when you background the app.
