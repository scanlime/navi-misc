begin
    prog = freevo
    button = preset
    config = SUBTITLE
end
begin
    prog = freevo
    button = ch-
    config = PREV_CH
end
begin
    prog = freevo
    button = cd
    config = EJECT
end
begin
    prog = freevo
    button = enter
    config = ENTER
end
begin
    prog = freevo
    button = prev
    config = REW
end
begin
    prog = freevo
    button = play
    config = PLAY
end
begin
    prog = freevo
    button = next
    config = FFWD
end
begin
    prog = freevo
    button = pause
    config = PAUSE
end
begin
    prog = freevo
    button = stop
    config = STOP
end
begin
    prog = freevo
    button = v5
    config = REC
end
begin
    prog = freevo
    button = up
    config = UP
end
begin
    prog = freevo
    button = down
    config = DOWN
end
begin
    prog = freevo
    button = guide
    config = DISPLAY
end
begin
    prog = freevo
    button = x
    config = EXIT
end
begin
    prog = freevo
    button = mute
    config = MUTE
end
begin
    prog = freevo
    button = right
    config = RIGHT
end
begin
    prog = freevo
    button = menu
    config = MENU
end
begin
    prog = freevo
    button = band
    config = SELECT
end
begin
    prog = freevo
    button = left
    config = LEFT
end
begin
    prog = freevo
    button = vol+
    config = VOL+
end
begin
    prog = freevo
    button = vol-
    config = VOL-
end
