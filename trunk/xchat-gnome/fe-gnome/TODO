* hook up the autoconnect column in the server prefs
* put automatically connected-to servers at the top of the list
* fix the about dialog such that it uses a real copyright symbol (check galeon's source for this)
* make the connect dialog check existing open networks before creating a new connection
* list accelerators in the context menus
* flesh out the userlist context menu
