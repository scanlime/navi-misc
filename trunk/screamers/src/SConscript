#!/usr/bin/env python

Import ('env')

common_sources = [
	'version.cpp',
]

screamers_sources = [
	'screamers.cpp',
]

screamersd_sources = [
	'screamersd.cpp',
]

env.Program ('screamers',  common_sources + screamers_sources)
env.Program ('screamersd', common_sources + screamersd_sources)
