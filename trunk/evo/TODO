set values for refresh counters
hook up _commit() in core
hook up _commit and _abort for plugins

come up with some method of representing separate city entries with the same station
	- could just combine them with / in xml

find duplicate/incorrect entries in xml file

change eds module to use Locations.xml and weather://ccf/STA
